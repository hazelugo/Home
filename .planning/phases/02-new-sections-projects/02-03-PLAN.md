---
phase: 02-new-sections-projects
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - index.html
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
must_haves:
  truths:
    - "Song Tool accordion entry exists above all security/network labs with Full-Stack tag, stack chips, live URL, and GitHub link"
    - "Workout App accordion entry exists with Full-Stack tag, stack chips, live URL, and GitHub link"
    - "Emma's Math Quest accordion entry exists with Frontend tag, stack chips, live URL, and GitHub link"
    - "Three new AI projects appear in DOM order BEFORE the five existing security/network labs"
    - "All five existing security/network lab projects remain present and functional (accordion opens/closes)"
    - "Project numbers are sequenced 01..08 in DOM order: Song Tool=01, Workout App=02, Emma=03, Vuln Lab=04, SIEM=05, Net Traffic=06, Segmented=07, A/V LAN=08"
    - "View live → and GitHub → links open in a new tab with rel=noopener noreferrer"
  artifacts:
    - path: "index.html"
      provides: "Three new <button class='project-item'> accordion entries plus a .project-links row pattern, plus renumbered existing project items"
      contains: "haze-song-tool.vercel.app"
  key_links:
    - from: "New project-item buttons"
      to: "toggleProject(this) handler"
      via: "onclick attribute"
      pattern: "onclick=\"toggleProject\\(this\\)\""
    - from: ".project-tag tag-fullstack / tag-frontend classes"
      to: "CSS tokens from Plan 01"
      via: "var(--tag-fullstack-*) / var(--tag-frontend-*)"
      pattern: "tag-fullstack|tag-frontend"
    - from: ".project-links row"
      to: "external GitHub and live URLs"
      via: "target=\"_blank\" rel=\"noopener noreferrer\""
      pattern: "target=\"_blank\""
---

<objective>
Add three new AI project accordion entries (Song Tool, Workout App, Emma's Math Quest) above the existing five security/network labs in the Projects section. Each new entry uses the existing `.project-item` accordion pattern verbatim plus a new `.project-links` row containing "View live →" and "GitHub →" links. Renumber the five existing projects so the full list reads 01..08 in DOM order.

Purpose: Surfaces the AI development track at the top of the Projects section while keeping all security/network credentials intact.
Output: index.html with eight project items in the correct order, the three new ones complete with descriptions, stack chips, category tags, and external links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-new-sections-projects/02-CONTEXT.md
@.planning/phases/02-new-sections-projects/02-01-SUMMARY.md
@.planning/phases/02-new-sections-projects/02-02-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .project-links CSS rule for the new external-link row</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: .project-tools / .tool-chip block ~lines 772-787)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-16, D-17, D-18, D-19)
  </read_first>
  <action>
    Per D-17, "View live →" and "GitHub →" anchors must be small, understated: font-size 0.75rem, color var(--accent), no underline by default, underline on hover. Add a single CSS rule block immediately AFTER the `.project-detail-text` rule and BEFORE the `.project-tools` rule (the .project-links row sits between these two in the HTML per D-16).

    Locate this existing block (around lines 765-776):
    ```
          .project-detail-text {
            font-size: 0.875rem;
            color: var(--text-muted);
            line-height: 1.72;
            margin-bottom: 1.1rem;
            max-width: 680px;
          }
          .project-tools {
    ```

    Insert these EXACT rules between `.project-detail-text { ... }` and `.project-tools {`:
    ```
          .project-links {
            display: flex;
            flex-wrap: wrap;
            gap: 1.25rem;
            margin-bottom: 1.1rem;
          }
          .project-links a {
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 0.04em;
            color: var(--accent);
            text-decoration: none;
            transition: color 0.15s;
          }
          .project-links a:hover {
            text-decoration: underline;
          }
    ```

    Match the existing 6-space indentation pattern of sibling rules. Do not modify .project-detail-text or .project-tools rules.
  </action>
  <verify>
    <automated>grep -q '\.project-links {' index.html && grep -q '\.project-links a {' index.html && grep -q '\.project-links a:hover {' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains '.project-links {'
    - index.html contains '.project-links a {'
    - index.html contains '.project-links a:hover {'
    - index.html contains 'color: var(--accent)' inside the .project-links a rule
    - index.html contains 'text-decoration: underline' inside the hover rule
    - The .project-links rules appear AFTER .project-detail-text and BEFORE .project-tools in source order
  </acceptance_criteria>
  <done>CSS rules for .project-links container and its anchors are added; styled per D-17 (accent color, 0.75rem, no underline default, hover underline).</done>
</task>

<task type="auto">
  <name>Task 2: Insert three new AI project accordion entries at top of projects-list</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: <div class="projects-list"> opener ~line 1484, and the first existing project-item button ~lines 1485-1525)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-13 through D-25)
  </read_first>
  <action>
    Insert three new `<button class="project-item">` accordion entries IMMEDIATELY after `<div class="projects-list">` opener and BEFORE the first existing project-item (the Vulnerability Assessment Lab entry, currently numbered 01).

    Each new entry uses the EXACT `.project-item` markup pattern from existing items, plus a new `.project-links` row inserted between `.project-detail-text` and `.project-tools` (per D-16). All three external links use `target="_blank" rel="noopener noreferrer"` (per D-19).

    Per D-14, the new project numbers are 01, 02, 03. Per D-23/D-24/D-25, write 3-5 sentences of first-person, specific description for each project, matching the voice and density of the existing project-detail-text paragraphs. Voice: first person, specific, no filler, no "passionate", no "leverage". Use the D-23/D-24/D-25 reference material to ground the content; do not copy verbatim.

    For Emma's Math Quest, REQUIREMENTS.md PROJ-03 specifies GitHub only (no live URL listed in PROJ-03). However CONTEXT.md D-18 specifies BOTH a live URL (https://emath.hazelugo.com) AND a GitHub repo. Honor the CONTEXT.md locked decision (D-18) — include BOTH links for Emma's Math Quest. The CONTEXT.md decision was made AFTER requirements were drafted and supersedes.

    Insert this EXACT block at the position described (replace each `[description prose]` with the actual 3-5 sentence description you write; everything else is verbatim):

    ```
                <button
                  type="button"
                  class="project-item reveal"
                  aria-expanded="false"
                  onclick="toggleProject(this)"
                >
                  <div class="project-row">
                    <span class="project-num" aria-hidden="true">01</span>
                    <div>
                      <h3 class="project-name">Song Tool</h3>
                      <div class="project-summary">
                        Music catalog and playlist builder with full-text search, drag-reorder, and defense-in-depth auth
                      </div>
                    </div>
                    <span class="project-tag tag-fullstack">Full-Stack</span>
                    <span class="project-toggle" aria-hidden="true">+</span>
                  </div>
                  <div class="project-expand">
                    <div class="project-expand-inner">
                      <div class="project-detail">
                        <p class="project-detail-text">
                          [Song Tool description — 3-5 sentences, first person. Reference per D-23: music catalog and playlist builder; technical decisions include JWT-based sessions, Postgres tsvector full-text search, fractional position indexing for drag-and-drop without cascading row updates, defense-in-depth auth (middleware + route guards + RLS), and Playwright E2E coverage. Voice: "Song Tool is a music catalog and playlist builder I built end-to-end..." Concrete, grounded.]
                        </p>
                        <div class="project-links">
                          <a href="https://haze-song-tool.vercel.app" target="_blank" rel="noopener noreferrer">View live →</a>
                          <a href="https://github.com/hazelugo/Song-Tool" target="_blank" rel="noopener noreferrer">GitHub →</a>
                        </div>
                        <div class="project-tools">
                          <span class="tool-chip">Next.js 16</span>
                          <span class="tool-chip">TypeScript</span>
                          <span class="tool-chip">Supabase</span>
                          <span class="tool-chip">Drizzle ORM</span>
                          <span class="tool-chip">Vercel</span>
                        </div>
                      </div>
                    </div>
                  </div>
                </button>

                <button
                  type="button"
                  class="project-item reveal reveal-d1"
                  aria-expanded="false"
                  onclick="toggleProject(this)"
                >
                  <div class="project-row">
                    <span class="project-num" aria-hidden="true">02</span>
                    <div>
                      <h3 class="project-name">Workout App</h3>
                      <div class="project-summary">
                        Multi-user workout tracker with Google OAuth, per-user row-level security, and Vercel SPA routing
                      </div>
                    </div>
                    <span class="project-tag tag-fullstack">Full-Stack</span>
                    <span class="project-toggle" aria-hidden="true">+</span>
                  </div>
                  <div class="project-expand">
                    <div class="project-expand-inner">
                      <div class="project-detail">
                        <p class="project-detail-text">
                          [Workout App description — 3-5 sentences, first person. Reference per D-24: started as a local viewer, evolved into a multi-user app. Per-user row-level security in Supabase, Google OAuth for sign-in, Vercel SPA routing configured so OAuth redirects resolve correctly. Voice: "Workout App started as a single-user viewer and evolved into..." Concrete, grounded.]
                        </p>
                        <div class="project-links">
                          <a href="https://workout.hazelugo.com" target="_blank" rel="noopener noreferrer">View live →</a>
                          <a href="https://github.com/hazelugo/Workout-App" target="_blank" rel="noopener noreferrer">GitHub →</a>
                        </div>
                        <div class="project-tools">
                          <span class="tool-chip">JavaScript</span>
                          <span class="tool-chip">Supabase</span>
                          <span class="tool-chip">Vercel</span>
                          <span class="tool-chip">Google OAuth</span>
                        </div>
                      </div>
                    </div>
                  </div>
                </button>

                <button
                  type="button"
                  class="project-item reveal reveal-d2"
                  aria-expanded="false"
                  onclick="toggleProject(this)"
                >
                  <div class="project-row">
                    <span class="project-num" aria-hidden="true">03</span>
                    <div>
                      <h3 class="project-name">Emma's Math Quest</h3>
                      <div class="project-summary">
                        Adaptive math practice game with synthesized audio, CSS confetti, and 60px touch targets — built for a real 6-year-old user
                      </div>
                    </div>
                    <span class="project-tag tag-frontend">Frontend</span>
                    <span class="project-toggle" aria-hidden="true">+</span>
                  </div>
                  <div class="project-expand">
                    <div class="project-expand-inner">
                      <div class="project-detail">
                        <p class="project-detail-text">
                          [Emma's Math Quest description — 3-5 sentences, first person. Reference per D-25: adaptive difficulty using a rolling 10-answer window targeting an 80% correct rate. Audio synthesized via Web Audio API (no external sound files). CSS confetti on correct streaks. 60px minimum touch targets for small hands. Built for a real 6-year-old user, not as a demo. Close with the specifics line per the "specifics" block in CONTEXT.md: "Built for a real user with real constraints — not a demo" or a close variant.]
                        </p>
                        <div class="project-links">
                          <a href="https://emath.hazelugo.com" target="_blank" rel="noopener noreferrer">View live →</a>
                          <a href="https://github.com/hazelugo/EmmaApp" target="_blank" rel="noopener noreferrer">GitHub →</a>
                        </div>
                        <div class="project-tools">
                          <span class="tool-chip">Vue 3</span>
                          <span class="tool-chip">Vite</span>
                          <span class="tool-chip">Tailwind CSS v4</span>
                          <span class="tool-chip">Web Audio API</span>
                        </div>
                      </div>
                    </div>
                  </div>
                </button>

    ```

    Place this entire block IMMEDIATELY after `<div class="projects-list">` and IMMEDIATELY before the existing `<button type="button" class="project-item reveal"` line that opens the Vulnerability Assessment Lab entry. Match the existing 12-space indentation of sibling project-item buttons.

    Per D-15: all three use the identical accordion pattern with `onclick="toggleProject(this)"` — the existing JS handler at the bottom of index.html will work automatically (no JS changes needed).
  </action>
  <verify>
    <automated>grep -q 'haze-song-tool.vercel.app' index.html && grep -q 'workout.hazelugo.com' index.html && grep -q 'github.com/hazelugo/Song-Tool' index.html && grep -q 'github.com/hazelugo/Workout-App' index.html && grep -q 'github.com/hazelugo/EmmaApp' index.html && grep -q 'class="project-tag tag-fullstack"' index.html && grep -q 'class="project-tag tag-frontend"' index.html && grep -q '>Song Tool<' index.html && grep -q '>Workout App<' index.html && grep -q "Emma" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains '>Song Tool<'
    - index.html contains '>Workout App<'
    - index.html contains "Emma" (the project name string for Emma's Math Quest)
    - index.html contains 'class="project-tag tag-fullstack">Full-Stack<' (appears at least twice — Song Tool and Workout App)
    - index.html contains 'class="project-tag tag-frontend">Frontend<' (once — Emma)
    - index.html contains 'href="https://haze-song-tool.vercel.app"'
    - index.html contains 'href="https://github.com/hazelugo/Song-Tool"'
    - index.html contains 'href="https://workout.hazelugo.com"'
    - index.html contains 'href="https://github.com/hazelugo/Workout-App"'
    - index.html contains 'href="https://emath.hazelugo.com"'
    - index.html contains 'href="https://github.com/hazelugo/EmmaApp"'
    - All six new external links carry target="_blank" rel="noopener noreferrer"
    - All three new buttons carry onclick="toggleProject(this)" and aria-expanded="false"
    - Stack chips appear inside .project-tools for each new project per D-23/D-24/D-25
    - Each project-detail-text contains 3-5 sentences of first-person grounded prose (no "passionate", no "leverage")
    - The three new project-item buttons appear BEFORE the Vulnerability Assessment Lab button in DOM order
  </acceptance_criteria>
  <done>Three new project-item accordion entries are inserted at the top of .projects-list with project-num 01/02/03, correct tags, complete .project-links rows, and stack chips. The pre-existing project items follow them in DOM order (still numbered 01-05 at this point — Task 3 renumbers them).</done>
</task>

<task type="auto">
  <name>Task 3: Renumber the five existing security/network project-num spans to 04-08</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: the five existing .project-item buttons in the Projects section, paying attention to their <span class="project-num"> values)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-14)
  </read_first>
  <action>
    The five existing security/network lab projects currently display project numbers 01, 02, 03, 04, 05 (in that DOM order):
    - Vulnerability Assessment Lab → currently 01 → change to 04
    - SIEM Log Analysis Lab → currently 02 → change to 05
    - Network Traffic Analysis → currently 03 → change to 06
    - Segmented Home Network Lab → currently 04 → change to 07
    - A/V Production LAN → currently 05 → change to 08

    These `.project-num` spans appear inside `<span class="project-num" aria-hidden="true">NN</span>`. Per D-14, update each one. Do this by walking the five existing project-item buttons in DOM order (Vuln Lab, SIEM, Net Traffic, Segmented, A/V) and updating ONLY the `.project-num` value inside each one.

    Critical: Task 2 added three NEW project-num spans with values 01, 02, 03 (for Song Tool, Workout App, Emma). Those must remain unchanged. Only the .project-num spans inside the five PRE-EXISTING security/network project-item buttons get updated.

    Specifically:
    1. The Vulnerability Assessment Lab's `<span class="project-num" aria-hidden="true">01</span>` → change `01` to `04`
    2. The SIEM Log Analysis Lab's `<span class="project-num" aria-hidden="true">02</span>` → change `02` to `05`
    3. The Network Traffic Analysis's `<span class="project-num" aria-hidden="true">03</span>` → change `03` to `06`
    4. The Segmented Home Network Lab's `<span class="project-num" aria-hidden="true">04</span>` → change `04` to `07`
    5. The A/V Production LAN's `<span class="project-num" aria-hidden="true">05</span>` → change `05` to `08`

    Use the surrounding `.project-name` (e.g., `<h3 class="project-name">Vulnerability Assessment Lab</h3>`) as a positional anchor to identify which `.project-num` belongs to which project. Do NOT change any other markup — descriptions, tags, tool chips, and external links for the existing projects remain unchanged. Per PROJ-05 all five existing labs must remain present and functional.

    Final DOM order after Plan 02-03 is fully applied:
    | DOM position | Project | project-num |
    |---|---|---|
    | 1 | Song Tool | 01 |
    | 2 | Workout App | 02 |
    | 3 | Emma's Math Quest | 03 |
    | 4 | Vulnerability Assessment Lab | 04 |
    | 5 | SIEM Log Analysis Lab | 05 |
    | 6 | Network Traffic Analysis | 06 |
    | 7 | Segmented Home Network Lab | 07 |
    | 8 | A/V Production LAN | 08 |
  </action>
  <verify>
    <automated>grep -c 'class="project-num" aria-hidden="true">01<' index.html && grep -c 'class="project-num" aria-hidden="true">08<' index.html && grep -q 'class="project-num" aria-hidden="true">04<' index.html && grep -q 'class="project-num" aria-hidden="true">07<' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">01<' (the Song Tool entry from Task 2)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">02<' (Workout App)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">03<' (Emma's Math Quest)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">04<' (Vulnerability Assessment Lab)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">05<' (SIEM Log Analysis Lab)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">06<' (Network Traffic Analysis)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">07<' (Segmented Home Network Lab)
    - index.html contains exactly ONE occurrence of 'class="project-num" aria-hidden="true">08<' (A/V Production LAN)
    - All five existing project descriptions, tags, tool chips remain unchanged in content
  </acceptance_criteria>
  <done>The five existing security/network labs are renumbered 04..08. Combined with the three new entries at 01..03, the Projects accordion now reads 01..08 in DOM order with the AI projects on top.</done>
</task>

</tasks>

<verification>
- Open index.html. Projects section displays eight items numbered 01..08 in correct order.
- Click each new project (Song Tool, Workout App, Emma's Math Quest): accordion opens, expanded content shows description, two external links ("View live →", "GitHub →"), and stack chip row.
- Click "View live →" on any new project: opens correct production URL in new tab.
- Click "GitHub →" on any new project: opens correct repo URL in new tab.
- Click each existing security/network lab: still opens, still shows original description and tool chips.
- The Full-Stack tag on Song Tool and Workout App renders teal (per tokens added in Plan 01).
- The Frontend tag on Emma's Math Quest renders rose/pink.
- Mobile 390px viewport: project rows display correctly (existing .project-row mobile rule hides tags below 768px — verify it still applies).
- No JS console errors.
</verification>

<success_criteria>
PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05 satisfied. All eight project accordion entries are functional. The three new AI projects lead, the five existing security/network labs follow with content unchanged. Phase 2 is complete: combined with Plans 01 and 02, every Phase 2 requirement (WKFL-01..04, PROJ-01..05, NAVM-01..02) is now implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-sections-projects/02-03-SUMMARY.md` per the summary template.
</output>
