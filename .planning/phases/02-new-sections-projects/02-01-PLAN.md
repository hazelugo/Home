---
phase: 02-new-sections-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - NAVM-01
  - NAVM-02
  - WKFL-04
must_haves:
  truths:
    - "Sidebar nav lists items in order: About, Skills, Workflow, Projects, Experience, Contact"
    - "A Workflow nav link with href='#workflow' exists between Skills and Projects"
    - "Section number labels read: About=01, Skills=02, Projects=04, Experience=05, Contact=06"
    - "CSS tokens for --tag-fullstack-* and --tag-frontend-* exist in :root"
  artifacts:
    - path: "index.html"
      provides: "Updated sidebar nav, renumbered section labels, new tag tokens"
      contains: "data-section=\"workflow\""
  key_links:
    - from: "sidebar nav anchor"
      to: "#workflow"
      via: "href attribute"
      pattern: "href=\"#workflow\""
    - from: ":root CSS block"
      to: "tag-fullstack / tag-frontend classes"
      via: "var(--tag-fullstack-bg) / var(--tag-frontend-bg)"
      pattern: "--tag-fullstack-bg"
---

<objective>
Lay the foundation for Phase 2: add CSS tokens for two new project tag styles (Full-Stack, Frontend), insert the Workflow item into the sidebar nav between Skills and Projects, and renumber the section display labels so the Workflow section (to be added in Plan 02) and the resequenced sections all read correctly.

Purpose: Plans 02 and 03 depend on the nav link target (#workflow), the renumbered section labels, and the tag tokens being present.
Output: index.html with updated :root tokens, sidebar nav, and section-num spans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-new-sections-projects/02-CONTEXT.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Full-Stack and Frontend tag CSS tokens to :root</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: :root block around lines 47-74, specifically the existing --tag-network-*, --tag-siem-*, --tag-av-* token declarations)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-20, D-21, D-22)
  </read_first>
  <action>
    In the `:root` CSS block in index.html, locate the existing tag tokens (lines starting with `--tag-network-bg`, `--tag-network-text`, `--tag-siem-bg`, `--tag-siem-text`, `--tag-av-bg`, `--tag-av-text`). Immediately AFTER the line `--tag-av-text: oklch(42% 0.15 145);` and BEFORE the closing `}` of the `:root` block, insert these EXACT four lines (matching the existing two-space indentation pattern of sibling token declarations):

    ```
            --tag-fullstack-bg: oklch(91% 0.07 185);
            --tag-fullstack-text: oklch(42% 0.15 185);
            --tag-frontend-bg: oklch(93% 0.06 0);
            --tag-frontend-text: oklch(48% 0.18 0);
    ```

    Also add the matching utility classes. Locate the existing `.tag-av { ... }` rule block (around lines 723-726). Immediately AFTER its closing `}`, insert these EXACT two CSS rules using the same indentation as `.tag-av`:

    ```
          .tag-fullstack {
            background: var(--tag-fullstack-bg);
            color: var(--tag-fullstack-text);
          }
          .tag-frontend {
            background: var(--tag-frontend-bg);
            color: var(--tag-frontend-text);
          }
    ```

    Do not modify any other existing token or class declaration. Per D-20, D-21, D-22.
  </action>
  <verify>
    <automated>grep -q "tag-fullstack-bg: oklch(91% 0.07 185)" index.html && grep -q "tag-frontend-bg: oklch(93% 0.06 0)" index.html && grep -q "\.tag-fullstack {" index.html && grep -q "\.tag-frontend {" index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains '--tag-fullstack-bg: oklch(91% 0.07 185)'
    - index.html contains '--tag-fullstack-text: oklch(42% 0.15 185)'
    - index.html contains '--tag-frontend-bg: oklch(93% 0.06 0)'
    - index.html contains '--tag-frontend-text: oklch(48% 0.18 0)'
    - index.html contains '.tag-fullstack {'
    - index.html contains '.tag-frontend {'
    - index.html contains 'background: var(--tag-fullstack-bg)'
    - index.html contains 'background: var(--tag-frontend-bg)'
  </acceptance_criteria>
  <done>Four new CSS custom properties and two new utility classes added inside :root and after .tag-av respectively; no existing token or class altered.</done>
</task>

<task type="auto">
  <name>Task 2: Insert Workflow nav item into sidebar between Skills and Projects</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: sidebar nav <ul> block around lines 1157-1183)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-10, D-11)
  </read_first>
  <action>
    Locate the sidebar nav `<ul>` block. The current order has these `<li>` items: About, Skills, Projects, Experience, Contact. Insert a new `<li>` for Workflow BETWEEN the Skills `<li>` and the Projects `<li>`.

    Find this exact block:
    ```
              <li>
                <a href="#skills" class="sidebar-item" data-section="skills"
                  >Skills</a
                >
              </li>
              <li>
                <a href="#projects" class="sidebar-item" data-section="projects"
                  >Projects</a
                >
              </li>
    ```

    Replace it with:
    ```
              <li>
                <a href="#skills" class="sidebar-item" data-section="skills"
                  >Skills</a
                >
              </li>
              <li>
                <a href="#workflow" class="sidebar-item" data-section="workflow"
                  >Workflow</a
                >
              </li>
              <li>
                <a href="#projects" class="sidebar-item" data-section="projects"
                  >Projects</a
                >
              </li>
    ```

    Do not modify the About, Experience, or Contact `<li>` items. Per D-10 and D-11. The IntersectionObserver in the existing script already targets `section[id]` — once Plan 02 adds `<section id="workflow">`, this nav item will scroll-link automatically.
  </action>
  <verify>
    <automated>grep -q 'href="#workflow" class="sidebar-item" data-section="workflow"' index.html && grep -c 'class="sidebar-item"' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains 'href="#workflow" class="sidebar-item" data-section="workflow"'
    - index.html contains '>Workflow</a'
    - index.html contains 'data-section="workflow"'
    - Sidebar nav contains 6 sidebar-item links (About, Skills, Workflow, Projects, Experience, Contact)
    - The Workflow li appears between the Skills li and the Projects li in source order
  </acceptance_criteria>
  <done>Sidebar nav has six items in the order About → Skills → Workflow → Projects → Experience → Contact. The Workflow nav link references #workflow with data-section="workflow".</done>
</task>

<task type="auto">
  <name>Task 3: Renumber section display labels for Projects, Experience, and Contact</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: .section-num spans in Projects section ~line 1481, Experience section ~line 1695, Contact section ~line 1773)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-12)
  </read_first>
  <action>
    Update the three `.section-num` display labels. About stays `01`, Skills stays `02`. The Workflow section (to be added in Plan 02) will display `03`. The existing three sections must shift up by one.

    1. In the Projects section header, find this exact line:
       ```
                  <span class="section-num" aria-hidden="true">03</span>
       ```
       which appears inside `<section id="projects" class="section section-alt">`. Change `03` to `04`.

    2. In the Experience section header, find this exact line:
       ```
                  <span class="section-num" aria-hidden="true">04</span>
       ```
       which appears inside `<section id="experience" class="section">`. Change `04` to `05`.

    3. In the Contact section header, find this exact line:
       ```
                  <span class="section-num" aria-hidden="true">05</span>
       ```
       which appears inside `<section id="contact" class="section">`. Change `05` to `06`.

    Do NOT change the About `01` or Skills `02` section-num values. Do NOT touch the `.highlight-num` spans in the About highlights block (those are 01/02/03 for separate highlight items, unrelated to section numbering). Do NOT change the `.project-num` values in Projects items (those are project sequence numbers, handled in Plan 03). Per D-12.

    Verify by counting: after this change, `.section-num` values across the page should read 01 (About), 02 (Skills), 04 (Projects), 05 (Experience), 06 (Contact). Workflow's `03` is added by Plan 02.
  </action>
  <verify>
    <automated>grep -c 'class="section-num" aria-hidden="true">04<' index.html && grep -c 'class="section-num" aria-hidden="true">05<' index.html && grep -c 'class="section-num" aria-hidden="true">06<' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains 'class="section-num" aria-hidden="true">04<' (Projects)
    - index.html contains 'class="section-num" aria-hidden="true">05<' (Experience)
    - index.html contains 'class="section-num" aria-hidden="true">06<' (Contact)
    - index.html still contains 'class="section-num" aria-hidden="true">01<' (About)
    - index.html still contains 'class="section-num" aria-hidden="true">02<' (Skills)
    - index.html does NOT contain 'class="section-num" aria-hidden="true">03<' yet (Workflow added in Plan 02)
  </acceptance_criteria>
  <done>Section-num display labels updated: Projects=04, Experience=05, Contact=06. About=01 and Skills=02 unchanged. The 03 slot is reserved for the Workflow section added in Plan 02.</done>
</task>

</tasks>

<verification>
- Sidebar nav contains six items in the exact order: About, Skills, Workflow, Projects, Experience, Contact
- :root has --tag-fullstack-bg, --tag-fullstack-text, --tag-frontend-bg, --tag-frontend-text tokens
- .tag-fullstack and .tag-frontend utility classes exist
- Section labels read: 01 About, 02 Skills, 04 Projects, 05 Experience, 06 Contact
- Page still loads with no JS errors (open index.html in a browser; check console)
</verification>

<success_criteria>
NAVM-01, NAVM-02, and the nav-link half of WKFL-04 are satisfied. CSS foundation for Plan 03's project tags is in place. Page is in a consistent state: nav references #workflow, but the Workflow section itself is added in Plan 02 — the nav link will inert until then (intentional).
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-sections-projects/02-01-SUMMARY.md` per the summary template.
</output>
