---
phase: 01-content-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
must_haves:
  truths:
    - "Hero subtitle reads 'AI, Security & Network Professional' (rendered, ampersand-encoded in source)"
    - "Hero description paragraph contains 'B.A.T.' and does NOT contain 'graduating B.S.' or any year qualifier"
    - "Hero description paragraph mentions shipped AI apps / production deployments alongside the existing 6+ years IT framing"
    - "Browser tab title and og:title reflect the dual-track (AI + security/network) identity"
    - "og:description no longer mentions 'Graduating May 2026' and surfaces AI development"
  artifacts:
    - path: "index.html"
      provides: "Updated hero role, hero description, title, og:title, og:description"
      contains: "AI, Security &amp; Network Professional"
  key_links:
    - from: "<title> element in <head>"
      to: "rendered browser tab"
      via: "static HTML"
      pattern: "<title>Hector Lugo —"
    - from: ".hero-role div"
      to: "rendered hero subtitle"
      via: "static HTML"
      pattern: "AI, Security &amp; Network Professional"
---

<objective>
Update the hero block and document meta (title + Open Graph tags) so the very first signal a visitor or social-card scraper receives reflects the dual-track AI + security/network identity. Removes the outdated "graduating" qualifier per D-02 and adds the AI sentence per D-03.

Purpose: HERO-01, HERO-02, HERO-03 — the dual-track repositioning starts at the top of the page and in metadata. Recruiters scanning the first 5 seconds, and any LinkedIn/social preview, must immediately see "AI + security/network", not the old cybersecurity-only framing.
Output: Modified `index.html` with updated `<title>`, `og:title`, `og:description`, `name="description"`, `.hero-role`, and `.hero-desc` content. No other sections touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-content-updates/01-CONTEXT.md
@CLAUDE.md
@index.html

<interfaces>
<!-- Exact strings currently in index.html — find these and replace as specified in tasks. -->

Line 6 (<title>):
```html
<title>Hector Lugo — Cybersecurity & Network Professional</title>
```

Lines 7-10 (name="description"):
```html
<meta
  name="description"
  content="Hector Lugo — CompTIA trifecta-certified IT professional based in South Texas. Security+, Network+, A+, ISC2 CC. 6+ years enterprise IT. Open to cybersecurity analyst and network administrator roles."
/>
```

Lines 11-14 (og:title):
```html
<meta
  property="og:title"
  content="Hector Lugo — Cybersecurity & Network Professional"
/>
```

Lines 15-18 (og:description):
```html
<meta
  property="og:description"
  content="CompTIA trifecta-certified. 6+ years enterprise IT. Graduating May 2026. Open to remote and South Texas opportunities."
/>
```

Lines 1224-1226 (.hero-role):
```html
<div class="hero-role">
  Cybersecurity &amp; Network Professional
</div>
```

Lines 1240-1244 (.hero-desc):
```html
<p class="hero-desc reveal reveal-d3">
  6+ years in enterprise IT, hands-on security lab work, and a
  graduating B.S. in Computer Information Technology. Bilingual.
  Mission-focused. Building systems that defend and connect.
</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hero role subtitle and hero description paragraph</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1222-1245 for the hero-meta-row and hero-desc)
    - .planning/phases/01-content-updates/01-CONTEXT.md (decisions D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Make TWO string replacements in index.html.

    REPLACEMENT 1 — Hero role subtitle (per D-01):
    Find this exact block (around line 1224-1226):
    ```html
              <div class="hero-role">
                Cybersecurity &amp; Network Professional
              </div>
    ```
    Replace with:
    ```html
              <div class="hero-role">
                AI, Security &amp; Network Professional
              </div>
    ```
    Note: the comma after "AI" is intentional. The ampersand MUST stay HTML-encoded as `&amp;`. Indentation (14 spaces before `<div>`) must be preserved.

    REPLACEMENT 2 — Hero description paragraph (per D-02, D-03, D-04):
    Find this exact block (around line 1240-1244):
    ```html
            <p class="hero-desc reveal reveal-d3">
              6+ years in enterprise IT, hands-on security lab work, and a
              graduating B.S. in Computer Information Technology. Bilingual.
              Mission-focused. Building systems that defend and connect.
            </p>
    ```
    Replace with:
    ```html
            <p class="hero-desc reveal reveal-d3">
              6+ years in enterprise IT, hands-on security lab work, and a
              B.A.T. in Computer Information Technology. Three shipped
              full-stack apps, AI-assisted development workflow, and production
              deployments on Vercel. Bilingual. Mission-focused. Building
              systems that defend and connect.
            </p>
    ```
    Why this exact copy: D-02 requires "B.A.T." with NO "graduating" qualifier and NO year. D-03 specifies the exact AI sentence ("Three shipped full-stack apps, AI-assisted development workflow, and production deployments on Vercel.") — use it verbatim, no tool name-dropping (per D-03). D-04 preserves the "Bilingual. Mission-focused. Building systems that defend and connect." closing.

    Do NOT modify any other element. Do NOT change the `.hero-sub` line ("CompTIA Trifecta Certified · ISC2 CC · South Texas") — D-05 keeps it unchanged. Do NOT change the `reveal reveal-d3` classes.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw index.html; if ($c -match 'AI, Security &amp; Network Professional' -and $c -match 'B\.A\.T\. in Computer Information Technology' -and $c -match 'Three shipped' -and $c -match 'production deployments on Vercel' -and $c -notmatch 'graduating B\.S\.' -and $c -match 'Bilingual\. Mission-focused\.') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AI, Security &amp; Network Professional" index.html` returns at least 1
    - `grep -c "Cybersecurity &amp; Network Professional" index.html` returns 1 or fewer (footer copy is handled later or kept; hero-role no longer matches this string)
    - `grep -c "graduating B.S." index.html` returns 0
    - `grep -c "B.A.T. in Computer Information Technology" index.html` returns 1
    - `grep -c "Three shipped" index.html` returns 1
    - `grep -c "production deployments on Vercel" index.html` returns 1
    - `grep -c "Bilingual. Mission-focused. Building" index.html` returns 1 (closing sentence preserved per D-04)
    - The string `class="hero-desc reveal reveal-d3"` still appears exactly once (reveal classes preserved)
  </acceptance_criteria>
  <done>
    Hero role reads "AI, Security & Network Professional". Hero description paragraph contains "B.A.T.", the AI sentence, and the unchanged closing — and contains no "graduating" qualifier.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update document title and Open Graph / description meta tags</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 6-18 for title and meta tags)
    - .planning/phases/01-content-updates/01-CONTEXT.md (decisions D-14, D-15)
  </read_first>
  <action>
    Make FOUR string replacements in index.html `<head>`.

    REPLACEMENT 1 — `<title>` element (per D-15):
    Find (line 6):
    ```html
        <title>Hector Lugo — Cybersecurity & Network Professional</title>
    ```
    Replace with:
    ```html
        <title>Hector Lugo — AI, Security & Network Professional</title>
    ```
    Note: inside a `<title>` element, raw `&` is acceptable browser behavior but use `&amp;` for strict XHTML conformance and consistency with the source's existing pattern (the original used raw `&` here, so we match that style — keep raw `&`).

    REPLACEMENT 2 — `name="description"` meta (per D-14):
    Find (lines 7-10):
    ```html
        <meta
          name="description"
          content="Hector Lugo — CompTIA trifecta-certified IT professional based in South Texas. Security+, Network+, A+, ISC2 CC. 6+ years enterprise IT. Open to cybersecurity analyst and network administrator roles."
        />
    ```
    Replace with:
    ```html
        <meta
          name="description"
          content="Hector Lugo — AI developer and CompTIA trifecta-certified IT professional in South Texas. Shipping full-stack AI apps; 6+ years enterprise IT, security, and network experience. Open to AI engineering, cybersecurity, and network roles."
        />
    ```

    REPLACEMENT 3 — `og:title` meta (per D-15):
    Find (lines 11-14):
    ```html
        <meta
          property="og:title"
          content="Hector Lugo — Cybersecurity & Network Professional"
        />
    ```
    Replace with:
    ```html
        <meta
          property="og:title"
          content="Hector Lugo — AI, Security & Network Professional"
        />
    ```

    REPLACEMENT 4 — `og:description` meta (per D-14):
    Find (lines 15-18):
    ```html
        <meta
          property="og:description"
          content="CompTIA trifecta-certified. 6+ years enterprise IT. Graduating May 2026. Open to remote and South Texas opportunities."
        />
    ```
    Replace with:
    ```html
        <meta
          property="og:description"
          content="AI developer shipping full-stack apps. CompTIA trifecta-certified, 6+ years enterprise IT. Based in South Texas, open to remote."
        />
    ```

    Why this copy: D-14 requires removing "Graduating May 2026" (outdated — degree complete per D-02) and surfacing AI development alongside security/network. D-15 requires the dual-track framing replacing the cybersecurity-only title. Voice rules from CLAUDE.md — sharp, grounded, capable; no "passionate", no "leverage", no "utilize" (none used). First person not applicable for meta tags (these are third-person descriptors).

    Do NOT modify `og:image`, `og:type`, `twitter:card`, `theme-color`, or any other head element.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw index.html; if ($c -match '<title>Hector Lugo — AI, Security &amp; Network Professional</title>' -and $c -match 'property=\"og:title\"\s*content=\"Hector Lugo — AI, Security &amp; Network Professional\"' -and $c -notmatch 'Graduating May 2026' -and $c -match 'AI developer shipping full-stack apps') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<title>Hector Lugo — AI, Security & Network Professional</title>" index.html` returns 1
    - `grep -c 'content="Hector Lugo — AI, Security & Network Professional"' index.html` returns 1 (og:title)
    - `grep -c "Graduating May 2026" index.html` returns 0
    - `grep -c "AI developer shipping full-stack apps" index.html` returns 1 (og:description)
    - `grep -c "AI developer and CompTIA trifecta-certified" index.html` returns 1 (name=description)
    - `grep -c 'property="og:image"' index.html` still returns 1 (image meta untouched)
    - `grep -c 'property="og:type"' index.html` still returns 1 (type meta untouched)
  </acceptance_criteria>
  <done>
    Browser tab title, og:title, og:description, and name=description all reflect the dual-track AI + security/network identity. No "Graduating May 2026" remains anywhere in the file's head section.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. Open index.html in browser — hero subtitle reads "AI, Security & Network Professional"
2. Hero paragraph reads "...B.A.T. in Computer Information Technology. Three shipped full-stack apps, AI-assisted development workflow, and production deployments on Vercel. Bilingual..."
3. Browser tab shows "Hector Lugo — AI, Security & Network Professional"
4. View page source — og:description contains "AI developer shipping full-stack apps"
5. No occurrence of "graduating B.S." or "Graduating May 2026" anywhere in source
</verification>

<success_criteria>
- HERO-01 satisfied: `.hero-role` div text is "AI, Security & Network Professional" (encoded `&amp;`)
- HERO-02 satisfied: `.hero-desc` mentions "B.A.T." with no "graduating" qualifier or year
- HERO-03 satisfied: `<title>`, `og:title`, `og:description`, and `name=description` all reflect dual-track identity
- No other sections, styles, or scripts modified
- File still parses as valid HTML (no broken tags)
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-updates/01-01-SUMMARY.md`
</output>
