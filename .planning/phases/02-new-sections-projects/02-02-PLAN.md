---
phase: 02-new-sections-projects
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - index.html
autonomous: true
requirements:
  - WKFL-01
  - WKFL-02
  - WKFL-03
  - WKFL-04
must_haves:
  truths:
    - "A 'How I work.' section appears in the page between Skills and Projects"
    - "The section has three named phase rows: Spec & Planning, Build, Quality Gate"
    - "Each phase row has 2-3 sentences of first-person, specific copy"
    - "The section number reads 03 and the section id is 'workflow'"
    - "The Workflow nav link added in Plan 01 now scroll-links to this section via IntersectionObserver"
  artifacts:
    - path: "index.html"
      provides: "<section id=\"workflow\"> with three .skill-row phase rows"
      contains: "id=\"workflow\""
  key_links:
    - from: "<section id=\"workflow\">"
      to: "sidebar nav data-section=\"workflow\""
      via: "id attribute match + IntersectionObserver"
      pattern: "id=\"workflow\""
    - from: "section.section markup"
      to: "between #skills and #projects"
      via: "DOM source order"
      pattern: "id=\"workflow\""
---

<objective>
Add the new "How I work." workflow section between the Skills and Projects sections. Use the existing `.skill-row` + `.skill-category` + `.skill-list` markup pattern (zero new CSS classes — pure reuse, per D-01). Three rows describe the three phases of the workflow: Spec & Planning (GSD), Build (Claude Code + MCPs), and Quality Gate (Impeccable).

Purpose: Communicates the AI-development methodology in the same typographic register as the Skills section — flat, no icons, no callout boxes.
Output: A new `<section id="workflow" class="section">` inserted into index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-new-sections-projects/02-CONTEXT.md
@.planning/phases/02-new-sections-projects/02-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Workflow section between Skills and Projects</name>
  <files>index.html</files>
  <read_first>
    - index.html (focus: end of Skills section ~line 1475 `</section>`, and the Projects section opener ~line 1478 `<section id="projects" class="section section-alt">`)
    - .planning/phases/02-new-sections-projects/02-CONTEXT.md (D-01 through D-09, plus specifics block)
    - .planning/PROJECT.md (voice notes: first person, specific, no filler, no "passionate"/"leverage")
  </read_first>
  <action>
    Insert a new `<section id="workflow" class="section">` block in index.html between the closing `</section>` of the Skills section (currently ends with `</section>` on the line immediately before `<!-- PROJECTS -->`) and the `<!-- PROJECTS -->` comment.

    Per D-01: reuse the EXACT `.skill-row` / `.skill-category` / `.skill-list` markup from the Skills section — no new CSS classes.
    Per D-02: heading text is `How I work.` (lowercase + period, matches sibling section voice).
    Per D-03: section uses `class="section"` (NOT `section-alt`) — keeps the About→Skills→Workflow→Projects→Experience→Contact alternation flowing correctly (Skills was plain `section`, Projects is `section-alt`, so Workflow must be plain `section` between them — wait: re-check. Looking at existing index.html: About uses `section section-alt` (line 1306), Skills uses `section` (line 1425), Projects uses `section section-alt` (line 1478). Adding Workflow between Skills and Projects means: Skills (plain) → Workflow (?) → Projects (alt). To maintain alternation we want plain alt plain alt. So Workflow must be `section section-alt`. HOWEVER per D-03 the user locked `class="section"` (no `section-alt`). Honor the user decision — D-03 is explicit. Use `class="section"`.)

    Per D-04: `.section-num` displays `03`.
    Per D-05: left-column labels are exactly "Spec & Planning", "Build", "Quality Gate" (use `&amp;` HTML entity for the ampersand).
    Per D-06, D-07, D-08, D-09: Write 2-3 sentences of first-person, specific copy for each row, matching the voice of the existing About paragraphs (concrete, grounded, no filler, no aspirational language). Use D-07/D-08/D-09 as reference material; do not copy verbatim.

    Insert this EXACT block (replace the bracketed `[...]` copy with the actual descriptions you write; everything else is verbatim):

    ```
          <!-- WORKFLOW -->
          <section id="workflow" class="section">
            <div class="container">
              <div class="section-header reveal">
                <span class="section-num" aria-hidden="true">03</span>
                <h2 class="section-heading">How I work.</h2>
              </div>
              <div class="skills-table">
                <div class="skill-row reveal">
                  <div class="skill-category">Spec &amp; Planning</div>
                  <div class="skill-list">
                    [2-3 sentences, first person, specific. Reference material per D-07: GSD is a meta-prompting + spec-driven development system. Structured discovery process, parallel research agents, atomic git commits, and a .planning/ directory of markdown files used as persistent memory across sessions. Mention the artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, CONTEXT.md per phase) only if it serves the sentence. Voice: "I run a structured discovery process before I write code..." Concrete, not aspirational.]
                  </div>
                </div>
                <div class="skill-row reveal reveal-d1">
                  <div class="skill-category">Build</div>
                  <div class="skill-list">
                    [2-3 sentences, first person, specific. Reference material per D-08: Claude Code is the primary coding agent running inside VS Code's integrated terminal. MCP orchestration brings in Supabase MCP, Gemini MCP, and Claude-native tools. Context across sessions is managed through the same markdown artifacts produced in Spec & Planning. Voice example: "Claude Code drives the implementation from VS Code's terminal..." No tool-namedrop chains — pick the names that earn their place.]
                  </div>
                </div>
                <div class="skill-row reveal reveal-d2">
                  <div class="skill-category">Quality Gate</div>
                  <div class="skill-list">
                    [2-3 sentences, first person, specific. Reference material per D-09: Impeccable is a 23-command design intelligence system. /teach profiles the audience, /audit runs a 5-dimension quality check producing findings with severity ratings, and individual commands resolve those findings. Voice: "Before anything ships, I run an Impeccable audit..." Close on the "nothing ships without passing the gate" beat.]
                  </div>
                </div>
              </div>
            </div>
          </section>

    ```

    Place this block IMMEDIATELY after the Skills section's closing `</section>` and IMMEDIATELY before the `<!-- PROJECTS -->` HTML comment that precedes the Projects section. Preserve exact indentation matching sibling sections (Skills, Projects).

    Voice constraints (from PROJECT.md and CLAUDE.md): first person, specific, no filler, no "passionate", no "leverage". Sharp, grounded, capable. Each row description: 2-3 sentences only. Do not exceed.
  </action>
  <verify>
    <automated>grep -q 'id="workflow"' index.html && grep -q 'How I work\.' index.html && grep -q 'Spec &amp; Planning' index.html && grep -q '>Build<' index.html && grep -q 'Quality Gate' index.html && grep -q 'class="section-num" aria-hidden="true">03<' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains 'id="workflow"'
    - index.html contains 'class="section"' on the workflow section opener (NOT 'section section-alt')
    - index.html contains 'How I work.'
    - index.html contains 'class="section-num" aria-hidden="true">03<' (the workflow section number)
    - index.html contains '>Spec &amp; Planning<' (D-05 label)
    - index.html contains '>Build<' inside a skill-category div (D-05 label)
    - index.html contains '>Quality Gate<' (D-05 label)
    - index.html contains three '.skill-row' divs inside the workflow section
    - The workflow section appears AFTER the closing </section> of #skills and BEFORE the <!-- PROJECTS --> comment in source order
    - Each .skill-list inside the workflow section contains 2-3 sentences of first-person prose (no "passionate", no "leverage", no aspirational language)
  </acceptance_criteria>
  <done>A new `<section id="workflow" class="section">` exists between Skills and Projects with header "How I work." (numbered 03) and three .skill-row phase entries. The sidebar nav link added in Plan 01 now scroll-links correctly because the IntersectionObserver picks up the new `section[id]` automatically.</done>
</task>

</tasks>

<verification>
- Open index.html in a browser. Workflow section appears between Skills and Projects.
- Scroll: the Workflow sidebar nav item becomes active when the Workflow section enters the viewport.
- Section labels in source order: 01 About, 02 Skills, 03 Workflow, 04 Projects, 05 Experience, 06 Contact.
- Three workflow rows visible, each with mono-uppercase left label and prose right column — visually identical to Skills section row treatment.
- Mobile (390px viewport): rows stack as single column (the existing @media (max-width: 768px) .skill-row rule handles this automatically).
- No new CSS rules added.
</verification>

<success_criteria>
WKFL-01, WKFL-02, WKFL-03, and the section-existence half of WKFL-04 are satisfied. The workflow section uses zero new CSS classes — pure reuse of the Skills pattern, matching the brand goal of restraint and typographic confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-sections-projects/02-02-SUMMARY.md` per the summary template.
</output>
