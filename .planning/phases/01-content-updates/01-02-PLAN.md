---
phase: 01-content-updates
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - ABUT-01
must_haves:
  truths:
    - "About section contains four paragraphs (was three): the original cybersecurity intro, the lab work paragraph, a new AI paragraph, and an updated availability paragraph"
    - "New AI paragraph covers shipped full-stack apps, AI-assisted workflow, and production deployments — without naming Song Tool, Workout App, or Emma's Math Quest"
    - "Third paragraph (now fourth) no longer says 'graduating May 2026' or 'make the full transition'"
    - "Bilingual mention and South Texas / open to remote context preserved in availability paragraph"
  artifacts:
    - path: "index.html"
      provides: "Updated About section with inserted AI paragraph and rewritten availability paragraph"
      contains: "AI-assisted"
  key_links:
    - from: ".about-text"
      to: "rendered About paragraphs"
      via: "static HTML"
      pattern: "AI-assisted"
    - from: "reveal animation chain"
      to: "scroll reveal observer"
      via: ".reveal class on new paragraph"
      pattern: "class=\"reveal reveal-d"
---

<objective>
Insert a new AI paragraph into the About section (as the third paragraph, per D-06) and rewrite the existing third paragraph (now fourth) to reflect the completed degree and current availability per D-10.

Purpose: ABUT-01 — the About section must communicate the AI development track to a hiring manager doing a deep read. Without this, the dual-track positioning is announced in the hero but never substantiated.
Output: Modified `index.html` with an inserted `<p class="reveal reveal-d2">` AI paragraph and a rewritten final `<p>` in `.about-text`. Reveal delay classes on subsequent paragraphs/blocks shift accordingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-content-updates/01-CONTEXT.md
@.planning/phases/01-content-updates/01-01-SUMMARY.md
@CLAUDE.md
@index.html

<interfaces>
<!-- Exact current state of the About paragraphs in index.html, lines 1313-1334. -->

```html
            <div class="about-text">
              <p class="reveal">
                I'm an IT professional with a dual focus in
                <strong>cybersecurity analysis</strong> and
                <strong>network administration</strong>, grounded in six years
                of day-to-day enterprise support at Mission CISD — one of the
                largest school districts in South Texas.
              </p>
              <p class="reveal reveal-d1">
                That environment demands real compliance rigor (FERPA, TEA),
                fast triage under operational pressure, and the ability to
                communicate clearly across every level of an organization. I
                brought that discipline into my home lab, where I've built out
                vulnerability assessment environments, SIEM pipelines, and
                segmented network architectures from scratch.
              </p>
              <p class="reveal reveal-d2">
                I'm fluent in English and Spanish, graduating May 2026, and
                looking to make the full transition into a dedicated security or
                network role where the work actually matters.
              </p>
              <div class="about-highlights reveal reveal-d3">
```

Voice constraints from CLAUDE.md and PROJECT.md:
- First person ("I", "I'm")
- No "passionate", "leverage", "utilize"
- Sharp, grounded, capable
- `<strong>` inline emphasis pattern (existing paragraphs use `<strong>cybersecurity analysis</strong>` etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert AI paragraph as third paragraph in About section</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1313-1335 for the .about-text block)
    - .planning/phases/01-content-updates/01-CONTEXT.md (decisions D-06, D-07, D-08, D-09)
    - CLAUDE.md (voice rules)
  </read_first>
  <action>
    Insert a NEW paragraph between the existing second paragraph (lab work, currently `reveal reveal-d1`) and the existing third paragraph (bilingual/availability, currently `reveal reveal-d2`). The new paragraph takes the `reveal reveal-d2` slot; the existing third paragraph and the highlights block both shift one delay step.

    Find this exact block (around lines 1321-1334):
    ```html
              <p class="reveal reveal-d1">
                That environment demands real compliance rigor (FERPA, TEA),
                fast triage under operational pressure, and the ability to
                communicate clearly across every level of an organization. I
                brought that discipline into my home lab, where I've built out
                vulnerability assessment environments, SIEM pipelines, and
                segmented network architectures from scratch.
              </p>
              <p class="reveal reveal-d2">
                I'm fluent in English and Spanish, graduating May 2026, and
                looking to make the full transition into a dedicated security or
                network role where the work actually matters.
              </p>
              <div class="about-highlights reveal reveal-d3">
    ```

    Replace with (inserting the new AI paragraph at `reveal-d2`, bumping the bilingual paragraph to `reveal-d3`, and bumping the highlights block to `reveal-d4`):
    ```html
              <p class="reveal reveal-d1">
                That environment demands real compliance rigor (FERPA, TEA),
                fast triage under operational pressure, and the ability to
                communicate clearly across every level of an organization. I
                brought that discipline into my home lab, where I've built out
                vulnerability assessment environments, SIEM pipelines, and
                segmented network architectures from scratch.
              </p>
              <p class="reveal reveal-d2">
                That same rigor carries into my
                <strong>AI development</strong> work. I've shipped three
                full-stack apps end-to-end — from schema design through
                production deployment — using an
                <strong>AI-assisted workflow</strong> built on Claude Code, MCP
                orchestration, and structured planning. The apps run live on
                Vercel with Supabase backends, real users, and the same
                operational discipline I apply to network and security work.
              </p>
              <p class="reveal reveal-d3">
                I'm fluent in English and Spanish, based in South Texas, and
                available now for full-time or contract roles — open to remote.
                The transition from enterprise IT into AI and security
                engineering is done; this is the work I'm doing.
              </p>
              <div class="about-highlights reveal reveal-d4">
    ```

    Why this exact copy:
    - D-06: AI paragraph inserted as third paragraph (after lab work, before bilingual)
    - D-07: 3-4 sentences, same density as existing paragraphs, grounded and specific
    - D-08: Covers shipped full-stack apps, AI-assisted workflow, production deployments; framed as natural extension of security/network background ("That same rigor carries into..."), not a pivot
    - D-09: Does NOT name Song Tool, Workout App, or Emma's Math Quest by title
    - D-10: Rewrites the bilingual paragraph — removes "graduating May 2026", removes "make the full transition" framing, keeps bilingual + South Texas + open to remote, present tense
    - Existing voice patterns preserved: `<strong>` inline emphasis on AI development and AI-assisted workflow (matches the `<strong>cybersecurity analysis</strong>` pattern in paragraph 1)
    - CLAUDE.md voice: no "passionate" or "leverage" used; first person; sharp and factual
    - Reveal delays cascade correctly: existing d1 → unchanged d1, NEW paragraph → d2, old d2 paragraph → d3, old d3 highlights → d4. The `.reveal-d4` class exists in the CSS (verified at line 993-995 of index.html).

    Do NOT modify the first paragraph (`<p class="reveal">`) or the about-side info blocks.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw index.html; if ($c -match 'That same rigor carries into' -and $c -match '<strong>AI development</strong>' -and $c -match '<strong>AI-assisted workflow</strong>' -and $c -match 'shipped three\s+full-stack apps end-to-end' -and $c -match 'Vercel with Supabase backends' -and $c -notmatch 'graduating May 2026' -and $c -notmatch 'make the full transition' -and $c -match 'available now for full-time or contract' -and $c -match 'about-highlights reveal reveal-d4') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "That same rigor carries into" index.html` returns 1
    - `grep -c "<strong>AI development</strong>" index.html` returns 1
    - `grep -c "<strong>AI-assisted workflow</strong>" index.html` returns 1
    - `grep -c "shipped three" index.html` returns 1
    - `grep -c "Vercel with Supabase backends" index.html` returns 1
    - `grep -c "graduating May 2026" index.html` returns 0 (was in About paragraph and contact-avail block — this task removes the About one; contact-avail block is out of phase scope and left intact)
    - `grep -c "make the full transition" index.html` returns 0
    - `grep -c "available now for full-time or contract" index.html` returns 1
    - `grep -c 'class="about-highlights reveal reveal-d4"' index.html` returns 1 (highlights bumped to d4)
    - `grep -c "Song Tool" index.html` returns 0 within the about-text block (project titles NOT in About per D-09 — they're added in Phase 2)
    - The first About paragraph (starts with "I'm an IT professional with a dual focus") is unchanged: `grep -c "dual focus in" index.html` returns 1
    - The second About paragraph (starts with "That environment demands") is unchanged: `grep -c "That environment demands real compliance rigor" index.html` returns 1
  </acceptance_criteria>
  <done>
    About section has four paragraphs in `.about-text`. The new third paragraph covers AI development as an extension of the security/network rigor, mentioning shipped full-stack apps, AI-assisted workflow, and Vercel/Supabase deployments. The fourth paragraph reflects completed degree and current availability. Reveal delays cascade correctly with `.about-highlights` now at `reveal-d4`.
  </done>
</task>

</tasks>

<verification>
After this task:
1. Open index.html in browser, scroll to About section
2. Confirm four paragraphs render in `.about-text`, each fading in sequentially
3. Third paragraph contains "AI development", "AI-assisted workflow", "shipped three full-stack apps", and ends mentioning Vercel/Supabase
4. Fourth paragraph reads naturally as "I'm fluent in English and Spanish, based in South Texas, available now..." — no "graduating", no "transition"
5. Highlights block (Security-first / Network architecture / Bilingual) still renders below paragraphs
</verification>

<success_criteria>
- ABUT-01 satisfied: About section has a paragraph covering shipped full-stack apps, AI-assisted workflow, and production deployments
- D-09 honored: no specific app titles (Song Tool, Workout App, Emma's Math Quest) appear in the About section
- D-10 honored: outdated "graduating May 2026" and "make the full transition" removed from About
- Reveal animation chain intact and visually staggered correctly
- No other sections modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-updates/01-02-SUMMARY.md`
</output>
