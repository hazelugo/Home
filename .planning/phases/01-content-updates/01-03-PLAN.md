---
phase: 01-content-updates
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - index.html
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
must_haves:
  truths:
    - "Skills section's first row is 'AI Development & Integration' with the exact tool list specified in D-12"
    - "Existing four skill rows (Networking, Security, Systems & OS, Hardware & Tools) remain unchanged in content and order"
    - "Skills table has exactly 5 .skill-row elements (was 4)"
    - "New AI row uses the same .skill-row / .skill-category / .skill-list pattern and reveal animation as existing rows"
  artifacts:
    - path: "index.html"
      provides: "Skills table with AI Development & Integration as first row, four existing rows preserved"
      contains: "AI Development &amp; Integration"
  key_links:
    - from: ".skills-table"
      to: "rendered skill rows"
      via: ".skill-row.reveal first child"
      pattern: "AI Development &amp; Integration"
---

<objective>
Prepend a new "AI Development & Integration" row to the skills table with the exact tool list from D-12, while leaving the four existing skill rows (Networking, Security, Systems & OS, Hardware & Tools) untouched per D-13. The AI row leads per D-11.

Purpose: SKIL-01 — the AI track must be enumerated in the skills table with concrete tools (Claude API, MCP orchestration, Next.js, Supabase, etc.) so a hiring manager evaluating depth has a credible inventory. SKIL-02 — the existing security/network skills must remain intact; this is additive, not a rewrite.
Output: Modified `index.html` with a new `.skill-row` prepended as the first child of `.skills-table`. Reveal delay classes on the four existing rows shift one step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-content-updates/01-CONTEXT.md
@.planning/phases/01-content-updates/01-02-SUMMARY.md
@CLAUDE.md
@index.html

<interfaces>
<!-- Exact current state of the skills table in index.html, lines 1419-1450. -->

```html
          <div class="skills-table">
            <div class="skill-row reveal">
              <div class="skill-category">Networking</div>
              <div class="skill-list">
                TCP/IP, Subnetting, VLANs, LAN/WAN, DHCP, DNS, Router Config,
                Switch Config, Wireshark, Network Troubleshooting
              </div>
            </div>
            <div class="skill-row reveal reveal-d1">
              <div class="skill-category">Security</div>
              <div class="skill-list">
                Access Control, Threat Detection, Vulnerability Assessment, SIEM
                / Splunk, Data Privacy, FERPA Compliance, TEA Regulations, Kali
                Linux, Nmap
              </div>
            </div>
            <div class="skill-row reveal reveal-d2">
              <div class="skill-category">Systems &amp; OS</div>
              <div class="skill-list">
                Windows Server, Windows Desktop, macOS, Linux (Kali), Active
                Directory, Group Policy, Virtualization
              </div>
            </div>
            <div class="skill-row reveal reveal-d3">
              <div class="skill-category">Hardware &amp; Tools</div>
              <div class="skill-list">
                PC Assembly &amp; Repair, Workstation Deployment, Peripheral
                Config, Mobile Devices, Microsoft 365, Ticketing Systems, Asset
                Management, VOIP Systems
              </div>
            </div>
          </div>
```

Pattern conventions (per code_context in CONTEXT.md):
- `.skill-category` uses `font-family: var(--font-mono)`, `color: var(--accent-2)`, uppercase
- `.skill-list` uses `color: var(--text-muted)`, `font-size: 0.95rem`, comma-separated
- Ampersands in category labels HTML-encoded as `&amp;`
- Reveal delay cascade: first row has just `.reveal`, subsequent rows get `.reveal-d1`, `.reveal-d2`, `.reveal-d3`
- `.reveal-d4` class exists in CSS (line 993-995) — used after prepend
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepend AI Development & Integration row and shift existing reveal delays</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1419-1450 for the .skills-table block)
    - .planning/phases/01-content-updates/01-CONTEXT.md (decisions D-11, D-12, D-13)
  </read_first>
  <action>
    Find this exact block (around lines 1419-1450):
    ```html
          <div class="skills-table">
            <div class="skill-row reveal">
              <div class="skill-category">Networking</div>
              <div class="skill-list">
                TCP/IP, Subnetting, VLANs, LAN/WAN, DHCP, DNS, Router Config,
                Switch Config, Wireshark, Network Troubleshooting
              </div>
            </div>
            <div class="skill-row reveal reveal-d1">
              <div class="skill-category">Security</div>
              <div class="skill-list">
                Access Control, Threat Detection, Vulnerability Assessment, SIEM
                / Splunk, Data Privacy, FERPA Compliance, TEA Regulations, Kali
                Linux, Nmap
              </div>
            </div>
            <div class="skill-row reveal reveal-d2">
              <div class="skill-category">Systems &amp; OS</div>
              <div class="skill-list">
                Windows Server, Windows Desktop, macOS, Linux (Kali), Active
                Directory, Group Policy, Virtualization
              </div>
            </div>
            <div class="skill-row reveal reveal-d3">
              <div class="skill-category">Hardware &amp; Tools</div>
              <div class="skill-list">
                PC Assembly &amp; Repair, Workstation Deployment, Peripheral
                Config, Mobile Devices, Microsoft 365, Ticketing Systems, Asset
                Management, VOIP Systems
              </div>
            </div>
          </div>
    ```

    Replace with (new AI row added as first child with `.reveal` only, existing rows each bumped by one delay step):
    ```html
          <div class="skills-table">
            <div class="skill-row reveal">
              <div class="skill-category">AI Development &amp; Integration</div>
              <div class="skill-list">
                Claude API / Anthropic SDK, Gemini CLI, MCP orchestration
                (Supabase MCP, Gemini MCP, Claude-native tools), GSD,
                Impeccable, Next.js 16 App Router, TypeScript, Drizzle ORM, Vue
                3, Vite, Supabase (PostgreSQL, auth, row-level security),
                Vercel, GitHub / version control, Context engineering via
                structured markdown
              </div>
            </div>
            <div class="skill-row reveal reveal-d1">
              <div class="skill-category">Networking</div>
              <div class="skill-list">
                TCP/IP, Subnetting, VLANs, LAN/WAN, DHCP, DNS, Router Config,
                Switch Config, Wireshark, Network Troubleshooting
              </div>
            </div>
            <div class="skill-row reveal reveal-d2">
              <div class="skill-category">Security</div>
              <div class="skill-list">
                Access Control, Threat Detection, Vulnerability Assessment, SIEM
                / Splunk, Data Privacy, FERPA Compliance, TEA Regulations, Kali
                Linux, Nmap
              </div>
            </div>
            <div class="skill-row reveal reveal-d3">
              <div class="skill-category">Systems &amp; OS</div>
              <div class="skill-list">
                Windows Server, Windows Desktop, macOS, Linux (Kali), Active
                Directory, Group Policy, Virtualization
              </div>
            </div>
            <div class="skill-row reveal reveal-d4">
              <div class="skill-category">Hardware &amp; Tools</div>
              <div class="skill-list">
                PC Assembly &amp; Repair, Workstation Deployment, Peripheral
                Config, Mobile Devices, Microsoft 365, Ticketing Systems, Asset
                Management, VOIP Systems
              </div>
            </div>
          </div>
    ```

    Why this exact content:
    - D-11: AI Development & Integration is the FIRST row (leads)
    - D-12: Tool list is verbatim — "Claude API / Anthropic SDK, Gemini CLI, MCP orchestration (Supabase MCP, Gemini MCP, Claude-native tools), GSD, Impeccable, Next.js 16 App Router, TypeScript, Drizzle ORM, Vue 3, Vite, Supabase (PostgreSQL, auth, row-level security), Vercel, GitHub / version control, Context engineering via structured markdown"
    - D-13: The four existing rows are byte-for-byte unchanged in their `.skill-list` text and category labels. Only their reveal delay class is shifted: Networking d0→d1, Security d1→d2, Systems d2→d3, Hardware d3→d4
    - Category label uses HTML-encoded `&amp;` to match existing pattern ("Systems &amp; OS", "Hardware &amp; Tools")
    - No new CSS needed: `.reveal-d4` already defined at line 993-995 of the source
    - `.skill-row:first-child { border-top: 1px solid var(--border); }` (line 635-637) automatically applies the top border to the new AI row — no CSS edits needed

    Do NOT modify the `<section id="skills">`, `.section-header`, or `.container` markup. Do NOT change `02` section number — that's Phase 2's job (NAVM-01).
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw index.html; $rowCount = ([regex]::Matches($c, 'class=\"skill-row reveal')).Count; if ($c -match 'AI Development &amp; Integration' -and $c -match 'Claude API / Anthropic SDK' -and $c -match 'MCP orchestration' -and $c -match 'Drizzle ORM' -and $c -match 'Context engineering via\s+structured markdown' -and $c -match 'class=\"skill-row reveal reveal-d4\"' -and $c -match '<div class=\"skill-category\">Hardware &amp; Tools</div>' -and $c -match 'TCP/IP, Subnetting, VLANs' -and $rowCount -eq 5) { 'PASS rowCount=' + $rowCount } else { 'FAIL rowCount=' + $rowCount }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AI Development &amp; Integration" index.html` returns 1
    - `grep -c "Claude API / Anthropic SDK" index.html` returns 1
    - `grep -c "Gemini CLI" index.html` returns 1
    - `grep -c "MCP orchestration" index.html` returns 1
    - `grep -c "Supabase MCP, Gemini MCP, Claude-native tools" index.html` returns 1
    - `grep -c "Next.js 16 App Router" index.html` returns 1
    - `grep -c "Drizzle ORM" index.html` returns 1
    - `grep -c "Supabase (PostgreSQL, auth, row-level security)" index.html` returns 1
    - `grep -c "Context engineering via" index.html` returns 1
    - Skills table contains exactly 5 `.skill-row` elements: `(grep -c "skill-row reveal" index.html)` returns 5
    - `grep -c "skill-row reveal reveal-d4" index.html` returns 1 (Hardware row bumped to d4)
    - `grep -c "skill-row reveal reveal-d3" index.html` returns 1 (Systems row at d3 — was at d2)
    - Existing skill lists unchanged: `grep -c "TCP/IP, Subnetting, VLANs, LAN/WAN" index.html` returns 1
    - Existing skill lists unchanged: `grep -c "Access Control, Threat Detection, Vulnerability Assessment" index.html` returns 1
    - Existing skill lists unchanged: `grep -c "Windows Server, Windows Desktop, macOS" index.html` returns 1
    - Existing skill lists unchanged: `grep -c "PC Assembly &amp; Repair, Workstation Deployment" index.html` returns 1
    - Skills section number still reads "02" (Phase 2 will renumber): `grep -c '<span class="section-num" aria-hidden="true">02</span>' index.html` returns 1
  </acceptance_criteria>
  <done>
    Skills table renders five rows in order: AI Development & Integration, Networking, Security, Systems & OS, Hardware & Tools. The AI row contains every tool from D-12 in the exact phrasing. All four pre-existing rows are byte-identical to their pre-task content except for their reveal delay class.
  </done>
</task>

</tasks>

<verification>
After this task:
1. Open index.html, scroll to Skills section
2. Confirm five rows render, AI Development & Integration first
3. AI row tools wrap naturally across multiple lines (long comma-separated list)
4. Each row fades in sequentially top-to-bottom on scroll into view
5. Mobile view (< 768px) — rows stack with category label above list, AI row first
</verification>

<success_criteria>
- SKIL-01 satisfied: "AI Development & Integration" category exists with the full D-12 tool list verbatim
- SKIL-02 satisfied: Networking, Security, Systems & OS, Hardware & Tools rows are content-identical to pre-task (only reveal delay class changed)
- Skills table has 5 rows total
- No CSS, JS, or other section changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-updates/01-03-SUMMARY.md`
</output>
